# Backend - XimByte

Aqui ficará a aplicação backend da plataforma. Pode ser feita com Flask ou Django.

## Estrutura sugerida
- `app/`: código-fonte da API
- `requirements.txt`: dependências do Python
- `run.py` ou `manage.py`: script de inicialização

Responsável: Luigi
